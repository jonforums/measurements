# directory configuration
# currently, directories must be relative to the root poject dir
:dirs:
  :workloads: workloads
  :input: input
  :logs: logs

# API tracer configuration
:tracer:
  :name: ProcessMonitor
  :active: true
  :exe: C:/winsystools/ProcessMonitor/procmon.exe

# Benchmarking configuration
:bench:
  :iterations: 3
